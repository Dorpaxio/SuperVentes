import { MontantTotalPipe } from './montant-total.pipe';

describe('MontantTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new MontantTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
