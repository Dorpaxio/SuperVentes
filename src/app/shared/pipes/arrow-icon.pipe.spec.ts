import { ArrowIconPipe } from './arrow-icon.pipe';

describe('ArrowIconPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrowIconPipe();
    expect(pipe).toBeTruthy();
  });
});
